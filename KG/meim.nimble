# Package
version       = "0.1.0"
author        = "MEIM Implementation"
description   = "Multi-partition Embedding Interaction iMproved for Knowledge Graph Link Prediction"
license       = "MIT"
srcDir        = "src"
bin           = @["meim"]

# Dependencies
requires "nim >= 1.6.0"
